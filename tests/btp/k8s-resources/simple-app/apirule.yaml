apiVersion: gateway.kyma-project.io/v2
kind: APIRule
metadata:
  name: simple-app-api
spec:
  gateway: kyma-system/kyma-gateway
  hosts:
    - simple-app
  rules:
    - methods:
        - GET
      noAuth: true
      path: /*
  service:
    name: simple-app-service
    port: 80
